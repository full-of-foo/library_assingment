<li class="content">
  <span><%= address.line1 %>,</span>

  <% if not address.line2.blank? %>
    <span><%= address.line2 %>,</span>
  <% end %>

  <span><%= address.city %>,</span>

  <% if not address.zip.blank? %>
    <span><%= address.zip %>,</span>

  <% end %>

  <span><%= address.state %>,</span>

  <span><%= address.country %></span>

   <% if customer_has_ownership?(current_user, address) %>
    <span class="pull-right">
        <%= link_to "Update", edit_customer_address_path(current_user, address), class: "btn btn-success" %>
        <% if current_user.addresses.count > 1 %>
          <%= link_to "Remove", customer_address_path(current_user, address), method: :delete, data: { confirm: "Will also delete purchase histories of this address! You sure?" }, class: "btn btn-danger" %>
        <% end %>
    </span>
  <% end %>


</li>
